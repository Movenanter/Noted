DATABASE_URL=sqlite:///./mentra.db
API_BEARER_TOKEN=devsecret123
WEBHOOK_TOKEN=mentra_webhook_secret

# Gemini config
GEMINI_API_KEY=replace_me
GEMINI_MODEL=gemini-2.5-flash
REQUEST_TIMEOUT_SECONDS=60

# Optional: Demo mode uses deterministic outputs and avoids external LLM calls
DEV_FAKE_LLM=0

# AgentMail inbound webhook HMAC secret
AGENTMAIL_WEBHOOK_SECRET=dev-demo-secret

# Base URL for scripts; set to your tunnel URL when using cloudflared/ngrok
# e.g. https://abc123.trycloudflare.com
# BACKEND_BASE and BASE are read by scripts/agentmail_e2e.py and glasses_ws_demo.py
BACKEND_BASE=http://127.0.0.1:8020
BASE=http://127.0.0.1:8020
